services:
  mongo:
    image: mongo:7
    container_name: tingilin-mongo
    ports:
      - "27017:27017"
    volumes:
      - tingilin_mongo_data:/data/db
    restart: unless-stopped

volumes:
  tingilin_mongo_data:
